---
layout: base.njk
title: "Theme Parks Directory - All Parks Worldwide"
description: "Browse our comprehensive directory of theme parks worldwide. From Universal Epic Universe to hidden international gems, find your next adventure."
permalink: /parks/
---

{# Breadcrumbs #}
<div class="bg-white border-b">
  <div class="container">
    <nav class="breadcrumbs" aria-label="Breadcrumb">
      <a href="/">Home</a>
      <span>/</span>
      <span aria-current="page">Parks</span>
    </nav>
  </div>
</div>

{# Page Header #}
<section class="bg-gradient-to-br from-accent-600 to-accent-500 text-white py-12 md:py-16">
  <div class="container text-center">
    <h1 class="text-fluid-4xl font-bold mb-4">Theme Parks Directory</h1>
    <p class="text-xl opacity-90 max-w-2xl mx-auto">
      Explore {{ parks.featured | length }}+ parks across 5 continents. From record-breaking coasters to immersive themed lands.
    </p>
  </div>
</section>

{# Filter by Region #}
<section class="bg-white sticky top-16 z-40 border-b">
  <div class="container">
    <div class="flex gap-2 py-4 overflow-x-auto scrollbar-hide">
      <a href="#all" class="whitespace-nowrap px-4 py-2 bg-primary-500 text-white rounded-full text-sm font-medium">
        All Parks
      </a>
      {% for regionId, region in parks.regions %}
        <a href="#{{ regionId }}" class="whitespace-nowrap px-4 py-2 bg-gray-100 hover:bg-gray-200 rounded-full text-sm font-medium transition-colors">
          {{ region.name }}
        </a>
      {% endfor %}
    </div>
  </div>
</section>

{# Featured Parks #}
<section class="section" id="all">
  <div class="container">
    <h2 class="text-fluid-2xl font-bold mb-2">Featured Parks</h2>
    <p class="text-gray-600 mb-8">The newest and most exciting destinations</p>

    <div class="park-grid">
      {% for park in parks.featured %}
        {% include "components/park-card.njk" %}
      {% endfor %}
    </div>
  </div>
</section>

{# Parks by Region #}
{% for regionId, region in parks.regions %}
<section class="section {% if loop.index is odd %}bg-gray-50{% else %}bg-white{% endif %}" id="{{ regionId }}">
  <div class="container">
    <div class="flex items-center justify-between mb-8">
      <div>
        <h2 class="text-fluid-2xl font-bold mb-2">{{ region.name }}</h2>
        <p class="text-gray-600">{{ region.description }}</p>
      </div>
      <a href="/parks/{{ regionId }}/" class="hidden md:inline-flex btn btn-secondary">
        View All
      </a>
    </div>

    <div class="park-grid">
      {% for park in parks.featured %}
        {% if park.region == regionId %}
          {% include "components/park-card.njk" %}
        {% endif %}
      {% endfor %}
    </div>

    <div class="md:hidden text-center mt-6">
      <a href="/parks/{{ regionId }}/" class="btn btn-secondary">
        View All {{ region.name }} Parks
      </a>
    </div>
  </div>
</section>
{% endfor %}

{# CTA Section #}
<section class="section bg-gradient-to-r from-secondary-500 to-secondary-400 text-white">
  <div class="container text-center">
    <h2 class="text-fluid-3xl font-bold mb-4">Can't Decide?</h2>
    <p class="text-xl opacity-90 mb-8 max-w-2xl mx-auto">
      Use our comparison tools to find the perfect park for your family, budget, and interests.
    </p>
    <a href="/comparisons/" class="btn bg-white text-secondary-600 hover:bg-gray-100 text-lg">
      Compare Parks
    </a>
  </div>
</section>
